enum MassageType { error, info, success, warning }

enum MassagePositionType { top, center, bottom }

///```dart
///enum MassageShowType {
///     fade,
///     bubbleSlide,
///     boxSlide,
///     bubble
///   }
/// ```
enum MassageShowType { fade, bubbleSlide, boxSlide, zooming }

enum MassageSlide { left, right, none }
